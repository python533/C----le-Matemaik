using namespace std;
int main(){
  setlocale(LC_ALL,"Turkish");
int p=2;
int n=2;
}


int main(){
  int prime[n+1]
  int prime2[p*2,n+1,p]
  int primes[]

  for (size_t i = 0; i < prime.length; i++) {
  while p *p  <= n:
  if (prime[p]) {
  for (size_t i = 0; i < prime2.length; i++) {
    prime[i]= False;
    p+=1;
  }
  while (n <len(prime)) {
    if (prime[n]) {
      primes[].append(n);
      n+=1;
      LFP=list[i]
      i -=1;
      t1=time.time();
    }
      std::cout << "Bu kadar Zamanda bu kadar sayı bulundu",t1-t0;
      std::cout << "LPF",LPF;

  }
  }
  }
}
